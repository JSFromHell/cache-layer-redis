{
  "name": "cache-layer-redis",
  "version": "1.0.0",
  "description": "Redis handler for node-cache-layer.",
  "main": "dist/index",
  "typings": "dist/index",
  "scripts": {
    "prepublishOnly": "npm run compile",
    "compile": "tsc -p ."
  },
  "bugs": {
    "url": "http://github.com/JSFromHell/cache-layer-redis/issues"
  },
  "repository": {
    "type": "git",
    "url": "http://github.com/JSFromHell/cache-layer-redis.git"
  },
  "keywords": [
    "cache",
    "cache-layer",
    "redis-cache",
    "nosql-cache",
    "express-cache",
    "response-cache"
  ],
  "author": "Carlos R. L. Rodrigues",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^8.0.0",
    "@types/redis": "^2.8.6",
    "typescript": "^2.6.1"
  },
  "files": [
    "dist/*"
  ],
  "dependencies": {
    "redis": "^2.8.0"
  }
}
